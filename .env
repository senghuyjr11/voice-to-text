# Get Gemini API key from: https://aistudio.google.com/app/apikey
GEMINI_MODEL=gemini-2.0-flash-exp
GEMINI_API_KEY=

DIABETES_CONFIG_PROMPT=Return ONLY one JSON object describing how to compute glycemic load and how to categorize it. Include:
- gl_bins: array of three cut points (numbers) that split GL into 4 categories (low..high).
- gl_messages: array of 4 strings, one per category, with placeholders {t0},{t1},{t2},{t0p1},{t1p1},{t2p1} acceptable.
- gi_percent_base: number to divide GI*carbs by (commonly 100).
Do not include explanations or prose.

GI_SOURCE_DEFAULT=https://glycemicindex.com/
